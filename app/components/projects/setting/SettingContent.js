"use client"

import SettingForm from "../../common/form/project/SettingForm"

export default function SettingContent({ projectId }){

    return (
        <SettingForm projectId={projectId}/>
    )
}